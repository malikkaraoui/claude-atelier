# Handoff — Review complète P1→P4 de claude-atelier

> Date : 2026-04-12
> Type : review
> Priorité : haute

---

## De : Claude (Opus 4.6)

### Contexte

`claude-atelier` est un package NPM (pas encore publié, v0.1.0) qui fournit
une configuration opinionated pour Claude Code : runtime rules, orchestration,
autonomie, sécurité, satellites par stack, le tout installable via
`npx claude-atelier init`.

En une session, 4 phases ont été livrées :

- **P1 (10 commits)** : scaffolding (package.json, LICENSE MIT, README bilingue,
  arborescence `src/fr/` + `src/en/`, CLI squelette)
- **P2 (9 commits)** : refactor core — `CLAUDE.md` réduit de 285→145 lignes,
  contradiction §5↔§12 résolue, refs cassées corrigées, §9/§10/§12/§15/§17/§18
  extraits en satellites, `settings.json` consolidé
- **P3 (20 commits)** : satellites complets — 5 ecosystem (skills, plugins,
  hooks, memory, qmd), 6 orchestration (modes, subagents, parallelization,
  models-routing, spawn-rules, mcp-lifecycle), 3 security (secrets-rules,
  pre-push-gate, emergency), 3 autonomy (permission-modes, night-mode,
  loop-watchers), scripts/pre-push-gate.sh enfin livré, templates .claudeignore
  + .gitignore, lint-refs + lint-length + doctor
- **P4 (4 commits)** : CI GitHub Actions, `doctor` réel (27 checks),
  `init` réel (32 fichiers installés, merge settings.json intelligent),
  stratégie EN (FR-only v0.2.0, EN v0.3.0)

État actuel : 44 commits, 33 fichiers dans `src/`, 1710 lignes de contenu FR,
0 fichiers `_legacy.md`, 0 refs cassées, doctor HEALTHY.

Repo : https://github.com/malikkaraoui/claude-atelier

### Question précise

**Quels sont les angles morts, incohérences ou faiblesses architecturales
que tu détectes dans l'état actuel du projet ?**

Focus souhaité :
1. Structure et découpage des fichiers — est-ce que la granularité est bonne ?
2. Le `init` installer — manque-t-il quelque chose pour être réellement
   utilisable sur un vrai projet ?
3. Le système de `doctor` — quels checks manquent ?
4. Qualité des satellites — est-ce que certains sont trop verbeux, trop
   vagues, ou dupliquent du contenu ?
5. Tout ce que je ne vois pas parce que je suis trop dedans.

### Fichiers à lire

```text
src/fr/CLAUDE.md                           (core runtime, 145 lignes)
src/fr/orchestration/subagents.md          (nouveau, catalogue subagents)
src/fr/ecosystem/skills.md                 (nouveau, skills system)
src/fr/autonomy/night-mode.md              (canonical, ex-dupliqué)
src/fr/security/secrets-rules.md           (patterns, audit, bootstrap)
src/templates/settings.json                (settings consolidé)
bin/cli.js                                 (CLI entry point)
bin/init.js                                (installer)
test/doctor.js                             (27 checks)
scripts/pre-push-gate.sh                   (gate 5 étapes)
package.json                               (manifest NPM)
docs/roadmap.md                            (plan multi-phases)
```

### Contraintes / hors scope

- Ne pas proposer de réécrire le core (`src/fr/CLAUDE.md`) — il est stabilisé
- Ne pas proposer d'ajouter du contenu EN maintenant (décision prise : v0.3.0)
- Ne pas proposer de changer la structure de dossiers (elle est verrouillée
  par `doctor` et les tests)
- Se concentrer sur ce qui **manque** ou ce qui est **fragile**, pas sur ce
  qui est déjà bien

---

## Réponse de : Copilot/GPT

<!-- Malik : copie cette section complète dans Copilot, attends sa réponse,
     et colle-la ici. Ensuite montre-moi ce fichier. -->

---

## Intégration

<!-- Je remplirai cette section après avoir lu la réponse de Copilot. -->
