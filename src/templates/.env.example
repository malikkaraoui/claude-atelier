# claude-atelier — Variables d'environnement locales
# Ce fichier n'est JAMAIS commité (listé dans .gitignore + .claudeignore)

# 21st.dev — Clé API usage (composants UI premium via Séréna)
# ATTENTION : ceci est la clé de FACTURATION (format an_sk_...), pas la clé MCP connexion.
# La clé MCP connexion est dans ~/.claude.json (gérée par `claude mcp add`).
# Free tier : 100 utilisations incluses, pay-as-you-go ensuite.
#
# 1. Créer un compte : https://21st.dev
# 2. Générer une clé : https://21st.dev/agents/api-keys
# 3. Coller ici (format attendu : an_sk_xxxx...xxxx) :
MAGIC_API_KEY=
